namespace = wtmnb_ships
ship_event = {
	id = wtmnb_ships.1000
	is_triggered_only = yes
	hide_window = yes
	trigger = {
		from = {
			is_ship_size = wtmnb_fortress
		}
	}
	immediate = {
		# 群星修仙传
		set_variable = {
			which = sxx_ship_state
			value = 10
		}
	}
}